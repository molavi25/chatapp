import { BinaryType, Packet, PacketType, RawData } from "engine.io-parser/build/esm/commons.js";
import { decodePacket } from "engine.io-parser/build/esm/decodePacket.js";
import { encodePacket, encodePacketToBinary } from "engine.io-parser/build/esm/encodePacket.js";
declare const encodePayload: (packets: Packet[], callback: (encodedPayload: string) => void) => void;
declare const decodePayload: (encodedPayload: string, binaryType?: BinaryType) => Packet[];
export declare function decodePacketFromBinary(data: Uint8Array, isBinary: boolean, binaryType: BinaryType): Packet;
export declare const protocol = 4;
export { BinaryType, Packet, PacketType, RawData, decodePacket, decodePayload, encodePacket, encodePacketToBinary, encodePayload };

